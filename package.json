{
  "name": "fantasy-football-app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ui": "bun run --cwd packages/frontend dev",
    "api": "bun run --cwd packages/api dev"
  }
}
