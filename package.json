{
  "name": "fantasy-football-app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ui": "bun run --cwd packages/frontend dev",
    "api": "bun run --hot packages/api/src/index.ts"
  }
}
